{
  "name": "art-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:github": "vite build --mode github",
    "preview": "vite preview",
    "deploy": "npm run build:github && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^8.0.0"
  },
  "dependencies": {
    "lottie-web": "^5.13.0",
    "swiper": "^12.1.3"
  }
}
